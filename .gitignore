# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === Ambientes / uv ===
.venv/
venv/

# === Django ===
db.sqlite3
db.sqlite3-journal
staticfiles/
media/

# === Segredos (transferidos na mão entre máquinas, fora do git e fora do cloud) ===
.env
.secrets/

# Documentos médicos pessoais (nome/CPF/RG) — só no disco local, fora do git e do cloud
docs/meds/

# === oh-my-claudecode (estado local da máquina) ===
.omc/

# === codegraph (índice local volátil; SQLite + WAL + socket — nunca versionar/sincronizar) ===
.codegraph/

# === Sistema operacional / editores ===
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# === brainstorming visual companion (mockups locais) ===
.superpowers/

# === frontend Svelte (build local; dist servido apos collectstatic) ===
apps/backend/frontend/node_modules/
apps/backend/frontend/dist/

# === backups de .env transferidos na mão (podem conter segredo) ===
.env.pre-lan-bak
*.bak

# === claire (git worktrees locais da ferramenta) ===
.claire/
