# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === Ambientes / uv ===
.venv/
venv/

# === Django ===
db.sqlite3
db.sqlite3-journal
staticfiles/
media/

# === Segredos (transferidos na mão entre máquinas, fora do git e fora do cloud) ===
.env
.secrets/

# Documentos médicos pessoais (nome/CPF/RG) — só no disco local, fora do git e do cloud
docs/meds/

# === oh-my-claudecode (estado local da máquina) ===
.omc/

# === codegraph (índice local volátil; SQLite + WAL + socket — nunca versionar/sincronizar) ===
.codegraph/

# === Sistema operacional / editores ===
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
